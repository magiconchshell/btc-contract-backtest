# Research / notebook / optimization extras
-r requirements-dev.txt

plotly>=5.18.0
jupyter>=1.0.0
scikit-learn>=1.3.0
statsmodels>=0.14.0

# Performance optimization (optional; not required by core engine/tests)
# Note: current llvmlite/numba build chain can block Python 3.12 environment setup on macOS x86.
# Install separately only when a real numba-accelerated path is introduced.
# numba>=0.58.0
